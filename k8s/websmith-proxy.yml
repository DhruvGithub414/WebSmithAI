apiVersion: apps/v1
kind: Deployment
metadata:
  name: websmith-proxy
  namespace: websmith-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: websmith-proxy
  template:
    metadata:
      labels:
        app: websmith-proxy
    spec:
      containers:
        - name: proxy
          image: websmith-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: websmith-proxy-svc
  namespace: websmith-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: websmith-proxy